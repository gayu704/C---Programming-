#include <stdio.h>
int main() {
    int m[3], i, top;
    for(i=0;i<3;i++) scanf("%d",&m[i]);
    top = m[0];
    for(i=1;i<3;i++)
        if(m[i]>top) top=m[i];
    printf("Topper Marks = %d", top);
    return 0;
}
