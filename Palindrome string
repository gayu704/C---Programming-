#include <stdio.h>
#include <string.h>
int main() {
    char s[10], r[10];
    strcpy(r,s);
    strrev(r);
    if(strcmp(s,r)==0)
        printf("Palindrome");
    else
        printf("Not Palindrome");
    return 0;
}
