#include <stdio.h>
int main() {
    int price[5] = {50,100,150,200,250}, key, i;
    printf("Enter price to search: ");
    scanf("%d", &key);
    for(i = 0; i < 5; i++)
        if(price[i] == key)
            printf("Item Found");
    return 0;
}
