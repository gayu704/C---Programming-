#include <stdio.h>
#include <string.h>
int main() {
    char pass[10];
    scanf("%s",pass);
    if(strlen(pass)>=6)
        printf("Valid Password");
    else
        printf("Invalid Password");
    return 0;
}
